<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">

    <!-- ============ APPENDERS ============ -->
    <Appenders>

        <!-- In console -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%style{%d{HH:mm:ss}}{cyan} %highlight{%-5p} %style{[%c{1}:%L]}{magenta} - %msg%n" />
        </Console>

        <!-- Ghi log framework Ecommerce -->
        <RollingFile name="EcommerceLog"
                     fileName="logs/ecommerce/Ecommerce.log"
                     filePattern="logs/ecommerce/Ecommerce-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n" />
            <Policies>
                <TimeBasedTriggeringPolicy />
            </Policies>
        </RollingFile>
    </Appenders>

    <!-- ============ LOGGERS ============ -->
    <Loggers>

        <!-- ecommerce -->
        <Logger name="com.Ecommerce" level="info" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="EcommerceLog" />
        </Logger>

        <!-- Root logger (default cho các class khác) -->
        <Root level="info">
            <AppenderRef ref="Console" />
        </Root>
    </Loggers>

</Configuration>
